
fn add(a: int, b: int, c: int): int {
    return a + b;
}

fn main(test: bool): int {
    let a: int = 5;
    let b = 6;
    // let add: int;
    // let c: int = add(a, b);
    // add(a, b);
    if (a == b) {
        return 12;
    } else {
        a += b;
    }
}

